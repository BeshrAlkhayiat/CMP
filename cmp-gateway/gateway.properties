# CMP Gateway Configuration
# Copy this file to gateway.properties and customize for your environment

# REST API Configuration
rest.baseUrl=https://localhost:5443/cema/ccm/svc/db.file/rest/v2
# ca.name should match a CA name returned by CEMA's GET /ca for the login account.
# The gateway resolves it against the server's CA list tolerantly (case-insensitive,
# spaces vs. hyphens/underscores), so "CEMA-User-CA" == "cema user ca". If no CA matches,
# startup fails fast with a message listing all available CAs and their templates.
ca.name=CEMA-User-CA
template.name=employee
# Leave lookup.name empty to use the direct template-based issuance endpoint
# /ca/{caName}/template/{tplName}/issue (no CEMA Global Lookup required).
# Set it to a GLOBAL lookup name only if auto-issue via /ca/auto-issue/{name} is desired.
lookup.name=

# Authentication Configuration
# Options: certificate or basic
auth.type=certificate
auth.username=admin
auth.login.user=TenantAdmin

# For basic authentication
auth.username=asdf
auth.password=asdf

# For certificate authentication (PKCS#12 format)
auth.keystore.path=D:\\Projects\\cema\\tomcat\\base\\conf\\ssl\\ClientAuthCert-CEMA-Admin.p12
auth.keystore.password=manager
auth.keystore.alias=cema-admin

# CMP Protection Secret (text format, used for MAC-based protection)
cmp.protection.secret=qwe

# CMP listener
cmp.server.port=9000
cmp.server.path=/cmp

# Central key generation defaults for CRMF requests without a subject public key
central.key.kind=RSA
central.key.size=2048
# central.key.curve=secp256r1

# Timing Configuration
retry.after.seconds=30
downstream.timeout.seconds=300

auth.truststore.path=C:\\Users\\bea\\Downloads\\cema-truststore.p12
auth.truststore.password=pass123
auth.truststore.type=PKCS12